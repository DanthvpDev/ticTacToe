window.addEventListener('DOMContentLoaded', ()=> {
    const StartGameButton = document.getElementById('startGameButton');
    const SymbolSelectionSection = document.getElementById('symbolSelection');
    const inputPlayerOneName = document.getElementById('playerOneName').textContent;
    const inputPlayerTwoName = document.getElementById('playerTwoName').textContent;

    let playerName;

    StartGameButton.addEventListener('click', ()=> {
        SymbolSelectionSection.style.display = 'none';
        
    })

})

